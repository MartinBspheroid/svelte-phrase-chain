/**
 * This is a test file for TypeScript so we can test that translations are working even if they're not in .svelte files.
 * 
 * 
 */



import { type TranslationKey} from '$lib/i18n.svelte';

export const SaveTest : TranslationKey = "common.footer"


