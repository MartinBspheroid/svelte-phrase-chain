export const i18nConfig = {
  // Storage options
  persistLocale: true,
  localStorageKey: 'app_locale',

  // Locale options
  fallbackLocale: 'en',

  // Debug options
  debug: true,
  translationsDir: '../translations',
};